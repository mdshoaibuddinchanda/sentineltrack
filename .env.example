# Sentinel API Endpoint
SENTINEL_HOST=http://localhost:8000

# PostgreSQL / PostGIS Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=sentinel
DATABASE_USER=sentinel
DATABASE_PASSWORD=sentinel_dev

# Model Configuration Paths
VEHICLE_MODEL_PATH=models/vehicle/yolo11m.pt
PLATE_MODEL_PATH=models/plate/production/best.pt

# Stream & Reconnect Timeouts
RTSP_CONNECT_TIMEOUT=10
STREAM_MAX_BACKOFF=30
